# ============================================
# MeetFlow Conference Platform - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# ============================================
# Supabase Configuration (Required)
# ============================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role Key - NEVER expose this to the client!
# Used only for server-side admin operations
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# Application Configuration (Required)
# ============================================
# Your app URL - used for redirects and email links
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Stripe Configuration (Optional - for payments)
# ============================================
# Get these from: https://dashboard.stripe.com/apikeys
# For testing, use test keys (sk_test_... and pk_test_...)
# For production, use live keys (sk_live_... and pk_live_...)

# Stripe Secret Key (server-side only)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe Publishable Key (client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Webhook Secret
# Get this from: https://dashboard.stripe.com/webhooks
# After creating webhook endpoint: https://yourdomain.com/api/stripe-webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ============================================
# Email Configuration (Optional)
# ============================================
# Admin email for receiving notifications
ADMIN_EMAIL=admin@yourdomain.com

# Resend API Key (if using Resend for emails)
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# ============================================
# Development Notes
# ============================================
# - All variables starting with NEXT_PUBLIC_ are exposed to the browser
# - Never commit .env.local to git (it's in .gitignore)
# - For production, set these in Vercel Environment Variables
# - See docs/VERCEL_DEPLOY.md for deployment instructions

# ============================================
# Upstash Redis Configuration (Optional - for Rate Limiting & Caching)
# ============================================
# Get these from: https://console.upstash.com/
# Create a Redis database and copy the REST URL and Token
# Free tier: 10,000 requests/day

# Upstash Redis REST URL
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io

# Upstash Redis REST Token
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token

# Note: If not configured, rate limiting and caching will be disabled
# The app will work normally (fail-open strategy)

# ============================================
# Upstash Redis Configuration (Optional - for Rate Limiting & Caching)
# ============================================
# Get these from: https://console.upstash.com/
# Create a Redis database and copy the REST URL and Token
# Free tier: 10,000 requests/day

# Upstash Redis REST URL
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io

# Upstash Redis REST Token
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token

# Note: If not configured, rate limiting and caching will be disabled
# The app will work normally (fail-open strategy)
